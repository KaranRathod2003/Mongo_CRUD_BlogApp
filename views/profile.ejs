<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="links flex gap-10 mb-2">
            <a href="/" class="text-blue-500">
                Add new account
            </a>
            <a href="/logout" class="text-red-500">
                Logout
            </a>
        </div>
        <hr>
        <h1 class="text-2xl mt-5">Hello, <%= user.name  %></h1>
        <h5 class="text-xsm text-zinc-500">@<%= user.username  %></h5>
            <form action="/createpost" class=" flex flex-col w-72 gap-3 mt-10" method="post">
                <h3>Create Post</h3>
                <textarea name="content" placeholder="add thoughts..." class="resize-none p-2 outline-none bg-transparent border-[1px] rounded-md border-zinc-400" id=""></textarea>
                <input type="submit" value="Post" class="p-2 w-1/2 bg-blue-500 text-white cursor-pointer rounded-md">
            </form>

        <h1 class="mt-5">Your posts</h1>
        <% user.posts.reverse().forEach((post) => { %>
            <div class="w-1/3 p-3 rounded-md border-[1px] bg-zinc-700 mb-2">
                <h3 class="text-blue-500 text-sm">@<%= user.username%></h3>
                <p class="text-xl"><%= post.content %></p>
                <small><%= post.likes.length%> likes</small>
                <div class="links flex gap-2 mt-2">
                    <a class="text-red-400 tracking-tight" href="/like/<%= post._id  %>">
                        <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                    </a>
                    <a class="text-yellow-400 tracking-tight" href="/edit/<%= post._id%>">Edit</a>
                    <a class="text-zinc-500 tracking-tight" href="/delete/<%= post._id%>">Delete</a>
                </div>
            </div>
       <% }); %>
    </div>
</body>

</html>
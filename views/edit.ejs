<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h1 class="text-2xl mt-5">Hello, <%= user.name  %></h1>
        <h5 class="text-xsm text-zinc-500">@<%= user.username  %></h5>
            <form action="/update/<%= post._id %>" class=" flex flex-col w-72 gap-3 mt-10" method="post">
                <h3>Edit Post</h3>
                <textarea name="content" placeholder="add thoughts..." class="resize-none p-2 outline-none bg-transparent border-[1px] rounded-md border-zinc-400" id=""><%= post.content  %></textarea>
                <input type="submit" value="Post" class="p-2 w-1/2 bg-blue-500 text-white cursor-pointer rounded-md">
            </form>
    </div>
</body>

</html>